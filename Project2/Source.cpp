#include "List.h"
#include <iostream>

using namespace std;

int main() {
   List L1;

   L1.Insert(10);
   L1.Display();

   L1.Insert(20);
   L1.Display();

   L1.Insert(30);
   L1.Display();

   L1.Insert(40);
   L1.Display();

   L1.Insert(50);
   L1.Display();

    return 0;
}
